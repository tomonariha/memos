<form method='get' action='memos/new'>
  <input type='submit' value='追加'>
</form>
<ul>
  <% @memos.each do |id, content| %>
  <li> <a href='/memos/<%= id.to_i %>'><%= CGI.escapeHTML(content['title']) %></a></li>
  <% end %>
</ul>
