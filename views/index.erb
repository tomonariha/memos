<form method='get' action='memos/new'>
  <input type='submit' value='追加'>
</form>
<ul>
  <% @memos.each do |memo| %>
  <li> <a href='/memos/<%= memo[0].to_i %>'><%= CGI.escapeHTML(memo[1]['title']) %></a></li>
  <% end %>
</ul>
